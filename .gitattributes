# Keep Unix wrapper scripts with LF so Linux CI / containers can execute them.
gradlew text eol=lf
*.sh text eol=lf
*.bat text eol=crlf
*.jar binary

# Nginx templates must stay LF: CRLF makes "$uri\r" → "invalid variable name".
frontend/nginx.conf.template text eol=lf
